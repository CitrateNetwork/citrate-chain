{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Typed parameters the authoring pipeline (QRM-S7.4) must produce for this template. The registry pins keccak256 of this file as paramSchemaHash; the factory records which schema a deployment was supposed to satisfy but deliberately does NOT evaluate it on chain \u2014 see GovernanceProtocolFactory's NatSpec.",
  "properties": {
    "board": {
      "solidityType": "bytes32[]"
    },
    "boardThreshold": {
      "solidityType": "uint8"
    },
    "envelopes": {
      "solidityType": "address"
    },
    "reviewWindow": {
      "solidityType": "uint64"
    },
    "timelock": {
      "solidityType": "uint64"
    }
  },
  "required": [
    "board",
    "boardThreshold",
    "envelopes",
    "reviewWindow",
    "timelock"
  ],
  "title": "ChangeControlBoard constructor parameters",
  "type": "object"
}
