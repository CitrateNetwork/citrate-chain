# The PRODUCTION recursive-fold verifier key (commd_fold_vk.bin) IS committed and shipped — the
# `commd-fold-verify` feature `include_bytes!`s it into the validator binary (citrate-chain#170
# activation). Dev / scratch keys (baked with the insecure --dev SRS) stay out of the tree.
*.dev.bin
