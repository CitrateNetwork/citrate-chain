# The baked verifier key is a large (~27 MB) generated artifact, produced by the bake_vk tool from a
# trusted-setup .ptau. It is NOT committed — a feature-ON build (`--features commd-fold-verify`)
# requires it to be generated locally/in-release first. See README.md.
*.bin
