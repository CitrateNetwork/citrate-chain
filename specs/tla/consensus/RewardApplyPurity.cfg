\* Committed-only model (models the SRP-S2 FIX: the enhanced/NodeLocal reward
\* path is removed, so every role settles reward from committed state).
\* Expectation: TLC finds NO error — all invariants hold.
SPECIFICATION Spec

CONSTANTS
    Roles = {producer, receiver, coldsync, restart, reorg}
    Validator = val
    Treasury = tr
    BasicReward = 9
    BasicTreasury = 1
    NodeLocalRewards = {12, 15}
    MaxBlocks = 3
    AllowNodeLocal = FALSE

INVARIANT TypeOK
INVARIANT Purity
INVARIANT RewardRootAgreement
INVARIANT BalanceConservation
