\* ============================================================================
\* EmbeddedModelCommitment TLC configuration — WP-B.1
\*
\* Sprint: P950-B / Phase B.1
\*
\* Small finite parameter set for tractable model-checking. Abstract
\* ContentValues (modeling distinct off-chain artifacts); abstract
\* MaxCommitmentBytes (modeling fixed-size on-chain commitment).
\* ============================================================================

SPECIFICATION Spec

INVARIANT TypeInv
INVARIANT CommitmentSound
INVARIANT ValidStatusImpliesMatch
INVARIANT CommittedBlockFullyVerified
INVARIANT TamperDetection
INVARIANT CommitmentStability
INVARIANT PerModelOnChainSizeBounded

CONSTANTS
  ModelIds = {m1, m2, m3}
  ContentValues = {c1, c2, c3, c4}
  MaxCommitmentBytes = 32
