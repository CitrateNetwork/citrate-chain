\* Buggy-allowed model (models `main` today: the enhanced/NodeLocal reward
\* path is reachable, chosen off transient node-local state rather than
\* committed consensus state).
\* Expectation: TLC PRODUCES a counterexample violating RewardRootAgreement —
\* two roles applying the same block compute different reward-settled roots
\* (the block-2209 split-brain). TypeOK and Purity still hold (the root stays
\* a pure function of committed `bal`; the divergence is in `bal` itself).
SPECIFICATION Spec

CONSTANTS
    Roles = {producer, receiver, coldsync, restart, reorg}
    Validator = val
    Treasury = tr
    BasicReward = 9
    BasicTreasury = 1
    NodeLocalRewards = {12, 15}
    MaxBlocks = 3
    AllowNodeLocal = TRUE

INVARIANT TypeOK
INVARIANT Purity
INVARIANT RewardRootAgreement
